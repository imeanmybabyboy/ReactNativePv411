import { StyleSheet } from "react-native";

const HomeStyle = StyleSheet.create({
  topBar: {
    width: '100%',
    height: 50,
    backgroundColor: '#555',
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
});